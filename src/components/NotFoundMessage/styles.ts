import styled from "styled-components";

export const Container = styled.h2 `
    color: black;
    display: flex;
    align-items: center;
    justify-content: center;

    > span {
        margin-right: 8px;
    }

    > svg {
        width: 100px;
        height: 100px;
    }
`